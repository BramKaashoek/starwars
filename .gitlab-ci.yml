stages:
  - test
  - cypress_test
  - build

test:
  stage: test
  image: cypress/base:10
  script:
    - yarn
    - yarn test

cypress_test:
  stage: cypress_test
  image: cypress/base:10
  script:
    - yarn
    - yarn cypress

build:
  stage: build
  image: cypress/base:10
  script:
    - yarn
    - yarn build
